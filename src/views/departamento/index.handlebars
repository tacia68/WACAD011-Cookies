<div class="col-lg-12">
            <div class="page-header d-flex justify-content-center align-items-center">
              <h1 id="tables">Departamentos</h1>
                 <div>
                <a class="btn btn-primary" href="/dept/create">+</a>
              </div>
         
            </div>

            <div class="bs-component">
              <table class="table table-hover">
                <thead>
                  <tr>
                    <th scope="col">Nome</th>
                    <th scope="col">Sigla</th>

                     <th scope="col">Ações</th> <!-- Nova coluna para ações -->
                    
                    
                  </tr>
                </thead>
                <tbody>
                  {{#each departamentos}}
                  <tr class="table-dark">
                    <th scope="row">{{name}}</th>
                    <td>{{sigla}}</td>
                    <td></td>
                    <td>
      <a class="btn btn-primary" href="/dept/update/{{id}}">Editar</a> <!-- Botão de edição -->
      <a class="btn btn-danger" href="/dept/{{id}}/remove">Remover</a> <!-- Botão de remoção -->
    </td>
                  
                  </tr>
                  {{/each}}
                </tbody>
              </table>
            </div><!-- /example -->
          </div>
    